[providers]
python = "3.13"

[phases.setup]
nixPkgs = ["python3", "python3.pkgs.pip"]

[phases.install]
cmds = ["pip install http-server -q 2>/dev/null || true"]

[start]
cmd = "python3 -m http.server 8080"
